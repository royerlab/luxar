### Fixed

- SSAA now suspends MSAA renderbuffer allocation while active, preventing oversized multisample framebuffers on high-DPR displays; the configured MSAA mode returns when SSAA is disabled.
- Added a DPR-probe regression check confirming that resize updates do not move the projected orbit target.
