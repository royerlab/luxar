#### Zenodo characteristic refreshes stop reporting unreadable archives as success (#2350)

`gen_zenodo_records.py --refresh` now writes every successful measurement before reporting pinned archives it could not read and exiting non-zero. Explicit `--archives-root` runs also report how many archives came from that staging tree and warn when none did, including the namespaced layout the command expects.
