#### Preserve mesh appearance and report LOD surface error

Mesh substitutive LOD can now preserve per-vertex colours and scalars during
decimation with `attribute_weight`. Each generated level also records a normalized
geometric-error bound, which the viewer reports separately from additive mixture
quality so surface fidelity is visible without engaging energy compensation.
