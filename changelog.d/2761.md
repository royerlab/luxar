#### Keep authored touch captions clear of the control rail

On phones and tablets, left-edge overlays now move beside the wider touch rail
instead of painting underneath it. Explicitly sized text keeps a readable
18-character measure while staying inside the viewport, including when its
authored position is near the right edge; center- and right-anchored overlays
keep their authored horizontal origin.
