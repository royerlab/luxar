### Fixed

- Make scene geometry additions transactional: a failure after a partition or LOD wrapper has started writing now removes the new subtree and restores scene children, bounds, warnings, and array-reference deduplication state. Finalization also rejects any childless `kind=partition` or `kind=lod` wrapper as a structural backstop.
