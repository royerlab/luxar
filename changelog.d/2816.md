#### Batch-fit workers read only their assigned uniform tile

Uniform batch-fit plans now record per-frame tile occupancy once and pass each worker its exact spatial region and non-empty-tile divisor. Local and Slurm workers avoid materializing the full source frame or rescanning sibling tiles, while per-tile cull retention is applied before writing so the option is no longer inert in batch jobs.
