#### Compile scenes directly to `.luxar.zarr.zip`

`LuxarZarrCompiler` now accepts a `.zarr.zip` output path, writes through a temporary directory store, and publishes a flat `ZIP_STORED` archive only after finalization succeeds. Failed authoring or packaging leaves no partial archive and preserves any previous output.
