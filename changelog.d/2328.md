#### Viewer fixtures now track their producer's import closure

The viewer's generated-fixture freshness gate now fingerprints
`generate_test_data.py` and its reachable local Python imports through the same
shared resolver used by demos and examples. Unrelated Luxar source edits no
longer force the full fixture corpus to regenerate, and the obsolete
package-wide production fingerprint has been removed.
