#### Make `content_hash` portable across platforms

Finite floats in hashed Zarr attributes are now persisted at 12 significant decimal digits before `content_hash` is computed. Equivalent builds therefore keep the same on-disk metadata and cache identity across libm and BLAS implementations, including derived viewer camera poses; recompiling a scene once after this change intentionally refreshes existing warm caches.
