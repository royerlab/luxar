#### Keep Layers-panel controls out of global keyboard shortcuts

Keyboard input aimed at controls inside the Layers panel now stays within the
panel, so range-slider Home/End and Shift+arrow behaviour is no longer cancelled
by the scene animation bindings. Escape still reaches panel dismissal and Tab
still performs normal focus traversal.
