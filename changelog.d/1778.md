### Fixed

- Resolve tiled normalization ranges on the denoised intensity basis, matching the data each tile actually fits.
- Resolve preprocess-mode batch floors from the completed denoised store and on-the-fly percentile floors after NLM calibration, while preserving one global level across the batch.
- Report a uniform batch slice as failed, and refuse to merge it, when an applied floor leaves every spatial tile empty instead of silently dropping the slice.
