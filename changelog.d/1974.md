#### Make nD keyboard dimension selection visible

Digit keys now report unavailable nD navigation targets with an on-screen toast, while the dimension-sliders panel shows which non-displayed dimension the `[` and `]` keys currently control. The help overlay also makes the navigable-subset numbering explicit.
