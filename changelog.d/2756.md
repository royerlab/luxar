### Fixed

- Made `batch-fit submit --parallel` size packed Slurm jobs against GPU process overhead and the target partition's node CPU/RAM capacity, scale the requested allocation per packed task, and isolate each worker's GPU and quality/thread budgets.
