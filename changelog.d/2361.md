#### Diagnose unreadable pinned fits during Zenodo checks

`gen_zenodo_records.py --check` now distinguishes a pinned fit archive that is present but unreadable from an archive absent from the machine, reports the affected path, and exits non-zero instead of suggesting that the existing file be fetched.
