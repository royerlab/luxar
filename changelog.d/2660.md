- Add `coarse="points"` to `add_points(substitutive_lod=...)`, producing blue-noise subsampled Points levels with blending-aware float32 HDR brightness compensation while preserving the existing Gaussian-lift default.
