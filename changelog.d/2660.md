#### Same-type coarse levels for Points

`add_points(substitutive_lod=...)` now accepts `coarse="points"`, producing blue-noise subsampled Points levels with blending-aware float32 HDR brightness compensation while preserving the existing Gaussian-lift default.
