#### Zenodo refresh distinguishes inaccessible archives from absent ones (#2368)

`gen_zenodo_records.py --refresh` now reports archive candidates that exist but
cannot be hashed as a permissions problem instead of counting their preserved
measurements as files that are not on the machine. These unverified bytes remain
non-failing, matching the `--check` contract.
