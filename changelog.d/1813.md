#### Reject unsupported content-batch denoising

`luxar gsplat batch-fit run/submit --tiling content --denoise` now fails during planning instead of emitting content-box tasks that silently fit the raw input. Hand-run `--plan-box` content workers also report the existing unsupported-flag warning rather than suppressing it.
