### Fixed

- Preserve actionable archive-container faults raised during chunk loading instead of treating them as one skippable leaf failure.
- Show the first recorded cause in the total scene-load failure toast instead of directing users to the console.
