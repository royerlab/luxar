#### Keep doctor diagnosis independent from its info report

`luxar gsplat doctor` now continues diagnosing when its optional info report
cannot load a legacy or invalid gsplat store, rather than exiting before the
diagnosis. The command also accepts `--bins` / `-b` to re-bin histograms.
