#### Keep doctor diagnosis independent from its info report

`luxar gsplat doctor` now continues diagnosing when its optional info report
cannot load a legacy or invalid gsplat store, while still reporting an
unsupported store format as an outstanding error. The command also accepts
`--bins` / `-b` to re-bin histograms.
