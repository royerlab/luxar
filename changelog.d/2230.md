#### Unstamped GSplat edits stay core-only

Scaling, filtering, cropping, dimension embedding, and additive-prefix views of
an unstamped `GSplatData` no longer import the optional SciPy-backed LOD package.
Datasets with authored LOD quality or energy stamps still load the extra and
refresh those stamps after affected rewrites.
