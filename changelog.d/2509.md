### Added

- Added foreground-weighted held-out PSNR selection to `luxar gsplat cal`, with reproducible mask metadata and PDF reports that follow the selected metric.

### Fixed

- Corrected calibration guidance: gain-over-predict-zero selects the same K* as min-max PSNR, so sparse and deconvolved volumes now point to the foreground-weighted metric instead.
