#### Keep main-thread log-scalar decoding consistent

Log-scalar arrays now use the same float32 decoding kernel on the main thread and in workers, preventing different ranges of one array from decoding to different values when worker routing or fallback changes.
