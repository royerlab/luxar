#### Keep main-thread log-scalar decoding consistent

Log-scalar arrays now use the same float32 operation order in the main-thread decoder and TypeScript worker fallback. The residual difference from the worker WASM kernel's Rust `expm1f` remains tracked by #1843.
