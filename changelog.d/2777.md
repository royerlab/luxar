#### Add opt-in chromatic GSplat LOD partitioning

Substitutive GSplat reduction now accepts `color_weight=` to discourage bins from
mixing different RGB chromaticities while retaining amplitude as the existing
intensity currency. The default zero weight preserves the previous partition and
serialized output. The visual LOD A/B suite now includes a measured GSplat bench.
