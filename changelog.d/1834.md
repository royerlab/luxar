#### Accept automatic gsplat device selection

Gaussian-splat fitting, rendering, comparison, calibration, LOD, and culling now accept `--device auto`, matching the advertised CLI and Python API behavior.
