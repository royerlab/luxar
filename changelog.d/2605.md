### Performance

- Cache partition geometry footprints between commits and reuse transformed bounding boxes, removing steady-state subtree walks and short-lived allocations from the per-frame frustum gate.
