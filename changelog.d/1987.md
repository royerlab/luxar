#### Keep streamed GSplat ladders in one coherent draw order

Progressive GSplat appends no longer combine the previous depth-sorted prefix with a newly loaded storage-order suffix while the fresh worker sort is pending. The texture upload remains suffix-only, but the enlarged node temporarily uses one identity permutation, preventing detached alpha-over ghost copies during additive-LOD loading.
