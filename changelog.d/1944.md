### Fixed

- Made the Datasets rail control and `O` shortcut open the dataset browser during the initial dataset load. Selecting another dataset before initialization finishes now reports a clear retry message without changing the configured source or starting an overlapping load.
