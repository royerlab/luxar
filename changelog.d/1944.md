### Fixed

- Kept the Datasets rail control and `O` shortcut responsive during startup. Dataset choices made before initialization finishes now leave the browser open with a retry hint, without changing the configured source or starting an overlapping load.
