#### Project statistics report CUDA and Go test coverage

The project statistics now show GPU-kernel tests as an explicit subset of the
Python suite and include the native Go launcher tests. Go modules run once each,
failed Go tests affect the overall health status, and incomplete measurements
still prevent partial reports from being published.
