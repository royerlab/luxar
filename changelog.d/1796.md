#### Culling no longer leaves unwritable empty LOD rungs

A gsplat reduction can now remove every splat from one step of an additive ladder
without losing the completed result at save time. Empty steps are dropped when
other rungs survive, and the remaining ladder's indices, counts, cumulative
cutpoints, and rung count are rewritten to match the arrays that reach disk.
