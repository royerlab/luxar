#### Place anti-aliasing settings below their enable controls

The rendering controls now place the SSAA and MSAA settings folders directly
below the toggles that enable them, so each control appears before the options
it governs.
