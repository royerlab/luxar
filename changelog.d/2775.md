#### Same-type merged point and line LOD levels

`coarse="points"` and `coarse="lines"` now accept `method="merge"` alongside the existing `subsample` mode. Merged levels preserve the authored node type and derive representative position and scalar radius or width from light-weighted combined second moments without materialising Gaussian-splat children.
