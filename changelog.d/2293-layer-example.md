### Added

- Added a standalone `LuxarLayer` host example, its public embed-contract specification, and
  Playwright coverage for real framebuffer output, camera-driven LOD selection, and nD GSplat
  updates.
