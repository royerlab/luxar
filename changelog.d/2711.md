#### Detect a stopped CUDA cadence

The weekly external-reference audit now fails when the repository's CUDA native cadence is disabled, never succeeds after bootstrap, or has not succeeded in three days, while continuing to report third-party reference findings without gating.
