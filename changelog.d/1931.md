#### Reject degenerate custom log-scalar encodings

The `log_scalar_uint8` and `log_scalar_uint16` custom encoders now reject data
without a finite, positive maximum instead of writing zero-range metadata that
the decoder cannot read.
