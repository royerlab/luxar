#### Parallel eager scene loading

The viewer now loads independent scene siblings and partition parts with bounded concurrency, substantially shortening first paint for request-heavy partitioned scenes while preserving authored child order and BSP depth-sort indices.
