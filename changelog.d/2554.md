#### Integral quantized arrays decode without truncation bias

The Python decoder now rounds reconstructed values before restoring an integral
`original_dtype`, matching nearest-integer quantization semantics instead of
systematically truncating values toward zero. Floating-point decoding and the
CUSTOM encoder's historical truncating write path are unchanged.
