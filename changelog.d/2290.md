#### Bound eager line-loading memory

The viewer now admits eager line siblings by their declared vertex-count working set as well as the existing eight-wide slot limit, preventing several million-vertex nodes from overlapping their decode, projection, staging, and geometry allocations while preserving full concurrency for small siblings.
