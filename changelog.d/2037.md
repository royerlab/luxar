#### Rebuild demo scenes after writer changes

Cached demo scenes now include Luxar's production Python sources in their builder
fingerprint, so encoder and store-writer changes invalidate scenes produced by older
code instead of silently reusing incompatible data.
