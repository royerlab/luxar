#### Rebuild demo scenes after writer changes

Cached demo scenes now include Luxar's production Python sources and Zarr writer
environment in their builder fingerprint, so encoder, store-writer, and format changes
invalidate scenes produced by older code instead of silently reusing incompatible data.
