#### Fixed

- Size merged-quality scoring against both host reference memory and the resolved CUDA device, including each local batch worker's share of free VRAM, instead of deciding GPU work from host RAM alone.
