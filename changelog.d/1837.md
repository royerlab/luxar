#### Validate partition BSP trees in the viewer

The scene loader now checks stored partition split planes against each part's
`position_bounds` before depth sorting uses them. Trees in a different coordinate
frame, malformed trees, and trees without trustworthy bounds are discarded so the
viewer falls back to centroid ordering instead of silently applying a wrong painter's
order. Overlapping tiled parts remain valid within their measured overlap band.
