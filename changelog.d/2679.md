#### Lines can stay lines through substitutive LOD

`add_lines(substitutive_lod=...)` now accepts `coarse="lines"`, producing seeded nested whole-polyline Lines levels with blending-aware width compensation and hidden-slice preservation while retaining lifted GSplats as the default.
