#### Honor Cache Overrides for Line Admission

The viewer now derives eager line-loading memory admission from the same explicit
cache-pool override, measured heap, device class, or fixed fallback used by the
cache tiers, so Safari and the native WKWebView launcher no longer remain pinned
to a 256 MiB line budget when a larger session budget is configured.
