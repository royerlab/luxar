#### Surface terminal dataset faults from `LuxarLayer`

Layer-mode hosts can now subscribe with `onDatasetFault()` or inspect the current terminal archive failure with `getDatasetFault()`. Fault subscriptions follow dataset switches and are released during teardown.
