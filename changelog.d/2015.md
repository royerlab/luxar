#### E2E runs rebuild stale generated examples

The Makefile E2E entrypoints now verify that `datasets/examples/` was generated
by the current example builders and Luxar Python writer code. Unchanged fixtures
remain a fast no-op, while producer changes, missing datasets, and leftover
datasets trigger a complete rebuild before Playwright starts.
