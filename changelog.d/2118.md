### Added

- Added a narrow `LuxarApp` keyboard-input API for custom contexts, bindings, input suspension, and live shortcut labels without exposing the input-handler implementation.
- Documented embedder shadowing, fallback, reset, keyboard-only, and capture-phase preemption contracts.

### Fixed

- Error guidance now reflects live dataset-browser and help shortcut bindings, with authored labels during early startup.
