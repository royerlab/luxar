#### Mesh shading gains an offset key light and specular controls

Mesh surfaces now use an above-left view-space key light and a subtle default
specular highlight, with `specular` and `shininess` available alongside the
existing mesh appearance controls in authored scenes and the Layers panel.
