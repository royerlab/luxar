#### Bound deferred-group retries after archive faults

Anonymous nested LOD branches now stop retrying an unreadable archive after three cooldown attempts, while ordinary transient subtree failures keep their existing recovery behaviour and reconnecting reopens the bounded retry path.
