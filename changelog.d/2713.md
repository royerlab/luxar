#### Correct the neuromast demo's physical Z scale

The two-channel neuromast rebuild recipe now uses the measured MetaMorph voxel
anisotropy, `0.25 / 0.1083 = 2.308402585...`, instead of the historical `2.5`
factor. This removes an approximately 8 % Z stretch from newly rebuilt archives.
