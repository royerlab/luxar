# TODOs:
- In the Python API we should be able to pass, for example, radiii, sharpness, color, etc. as constants (single float, or single color) instead of *having* to pass arrays every single time.
- 