Environment Package
===================

Tools for attaching a baked scene environment to a Luxar store. The environment
lights meshes authored with ``material="physical"`` and is stored as a root-level
sidecar rather than as scene geometry.

.. automodule:: luxar.environment
   :members:
   :undoc-members:
   :show-inheritance:

See Also
--------

* :doc:`/guides/user/CLI_REFERENCE` — the ``luxar env bake`` and
  ``luxar env attach`` commands
* :doc:`mesh` — the geometry type that consumes scene environments
