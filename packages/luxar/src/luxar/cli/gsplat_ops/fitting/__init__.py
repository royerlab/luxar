"""``luxar gsplat`` fitting commands — ``fit`` / ``cal`` / ``render`` / ``denoise``.

Owns the single-volume fitting path: seeding/tiling/recipe plumbing, blind-spot
calibration, and the volume render/denoise utilities. ``commands.py`` is the
registration surface (``register_fitting_commands``).
"""
