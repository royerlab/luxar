"""``luxar gsplat`` edit commands operating on a fitted ``.gsplats.zarr``.

Owns ``transform`` / ``merge`` / ``cull`` / ``filter`` / ``slice`` /
``partition`` / ``flatten`` / ``additive`` plus the shared option-parsing
helpers. ``commands.py`` is the registration surface
(``register_transforms_commands``).
"""
