{
  "name": "luxar-viewer",
  "version": "2025.8.3",
  "description": "GPU-accelerated WebGL renderer for nD scientific datasets",
  "private": true,
  "license": "BSD-3-Clause",
  "scripts": {
    "dev": "vite",
    "build": "pnpm build:wasm && tsc --project tsconfig.build.json && vite build",
    "build:lib": "pnpm build:wasm && vite build --config vite.lib.config.ts && tsc --project tsconfig.lib.json",
    "build:lib:check": "node scripts/check-lib-exports.mjs",
    "build:wasm": "bash scripts/build-wasm.sh",
    "build:wasm:dev": "bash scripts/build-wasm.sh --dev",
    "test:wasm": "cd src/wasm/rust && cargo test",
    "bench:wasm": "npx tsx src/tests/benchmarks/wasm-benchmark.ts",
    "preview": "vite preview",
    "test:generate-fixtures": "cd ../.. && hatch run python packages/luxar-viewer/tests/fixtures/generate_test_data.py",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage",
    "test:watch": "vitest --watch",
    "test:with-fixtures": "pnpm test:generate-fixtures && pnpm test",
    "test:e2e": "playwright test",
    "test:e2e:ci": "playwright test --grep-invert 'toHaveScreenshot' --retries 2",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:debug": "playwright test --debug",
    "test:e2e:report": "playwright show-report",
    "readme-images": "playwright test --config playwright.screenshots.config.ts generate-readme-images",
    "readme-videos": "playwright test --config playwright.videos.config.ts",
    "doc-images": "playwright test --config playwright.screenshots.config.ts generate-doc-images",
    "agent:debug": "ts-node tools/agent-driver.ts",
    "agent:debug:visible": "ts-node tools/agent-driver.ts --headless=false",
    "typedoc": "typedoc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts,.tsx",
    "format": "prettier --write src",
    "check": "pnpm run typecheck && pnpm run lint && pnpm test --run"
  },
  "dependencies": {
    "comlink": "^4.4.2",
    "fflate": "^0.8.2",
    "mediabunny": "^1.40.1",
    "postprocessing": "^6.38.0",
    "three": "^0.163.0",
    "zarrita": "^0.5.4"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@playwright/test": "^1.57.0",
    "@types/node": "^24.10.1",
    "@types/stats.js": "^0.17.4",
    "@types/three": "^0.178.1",
    "@typescript-eslint/eslint-plugin": "^8.48.0",
    "@typescript-eslint/parser": "^8.48.0",
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "@zarrita/storage": "^0.1.3",
    "eslint": "^9.39.1",
    "globals": "^16.5.0",
    "jsdom": "^24.1.3",
    "prettier": "^3.6.2",
    "stats.js": "^0.17.0",
    "ts-node": "^10.9.2",
    "typedoc": "^0.28.18",
    "typescript": "^5.9.3",
    "vite": "^7.2.4",
    "vitest": "^3.2.4"
  }
}
