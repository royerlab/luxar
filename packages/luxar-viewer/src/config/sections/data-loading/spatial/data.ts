import type { DataLoadingSpatialConfig } from './types';

/**
 * Data loading spatial configuration
 */
export const dataLoadingSpatialConfig: DataLoadingSpatialConfig = {
  defaultTolerance: 0.1, // Default tolerance for nD slicing
  defaultMaxRadius: 0.1, // Default max radius for spatial queries
};
