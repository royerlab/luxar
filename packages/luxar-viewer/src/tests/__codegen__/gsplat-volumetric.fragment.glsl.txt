#version 300 es

// Three.js r185 - Node System


// extensions


// precision

precision highp float;
precision highp int;
precision highp sampler2D;
precision highp sampler3D;
precision highp samplerCube;
precision highp sampler2DArray;

precision highp usampler2D;
precision highp usampler3D;
precision highp usamplerCube;
precision highp usampler2DArray;

precision highp isampler2D;
precision highp isampler3D;
precision highp isamplerCube;
precision highp isampler2DArray;

precision highp sampler2DShadow;
precision highp sampler2DArrayShadow;
precision highp samplerCubeShadow;


// structs

layout( location = 0 ) out vec4 fragColor;



// uniforms

layout( std140 ) uniform object {
	vec2 nodeUniform0;
	float nodeUniform1;
	float nodeUniform2;
	float nodeUniform3;
	float nodeUniform4;
	float nodeUniform5;
	float nodeUniform6;
	float nodeUniform7;
	float nodeUniform8;
	float nodeUniform9;
	float nodeUniform10;
	float nodeUniform11;
	mat3 nodeUniform13;
	uint nodeUniform14;
	mat3 nodeUniform15;
	uint nodeUniform16;
	mat3 nodeUniform17;
	uint nodeUniform18;
	mat3 nodeUniform19;
	uint nodeUniform20;
	mat4 nodeUniform22;
	float nodeUniform23;
	float nodeUniform24;
	vec2 nodeUniform25;
	float nodeUniform26;
	float nodeUniform27;
	float nodeUniform28;
	float nodeUniform29;
	float nodeUniform30;
	float nodeUniform31;
	float nodeUniform33;
	float nodeUniform34;
	float nodeUniform35;
	float nodeUniform36;
};


// varyings
in vec3 nodeVarying0;
in float nodeVarying1;
in float nodeVarying2;
in vec3 nodeVarying3;
in vec2 nodeVarying4;


// vars
vec4 DiffuseColor;
float nodeVar2;
float nodeVar3;
float nodeVar4;
float nodeVar5;
float nodeVar6;
vec3 nodeVar8;
float nodeVar9;
vec4 Output;
vec4 nodeVar10;

// codes


void main() {

	// flow
	// code

	nodeVar2 = ( ( vec2( vec2( gl_FragCoord.xy.x, nodeUniform0.y - gl_FragCoord.xy.y ).x, ( nodeUniform0.y - vec2( gl_FragCoord.xy.x, nodeUniform0.y - gl_FragCoord.xy.y ).y ) ) - nodeVarying4 ).x * nodeVarying3.x );
	nodeVar3 = ( ( ( vec2( vec2( gl_FragCoord.xy.x, nodeUniform0.y - gl_FragCoord.xy.y ).x, ( nodeUniform0.y - vec2( gl_FragCoord.xy.x, nodeUniform0.y - gl_FragCoord.xy.y ).y ) ) - nodeVarying4 ).y - ( nodeVarying3.y * nodeVar2 ) ) * nodeVarying3.z );
	nodeVar4 = ( ( nodeVar2 * nodeVar2 ) + ( nodeVar3 * nodeVar3 ) );

	if ( ( nodeVar4 > nodeUniform1 ) ) {

		discard;


	}

	nodeVar5 = ( ( ( nodeVarying2 * nodeUniform2 ) * max( ( exp( ( nodeVar4 * -0.5 ) ) - nodeUniform3 ), 0.0 ) ) * mix( 1.0, ( - log( ( 1.0 - min( nodeVarying1, 0.998046875 ) ) ) ), nodeUniform4 ) );

	if ( ( ( nodeVar5 * max( nodeUniform5, 1.0 ) ) < 0.0001 ) ) {

		discard;


	}

	nodeVar6 = ( ( nodeUniform6 * nodeUniform7 ) * nodeVar5 );
	nodeVar8 = max( ( ( nodeVarying0 * vec3( nodeUniform5 ) ) + vec3( nodeUniform8 ) ), vec3( 0.0, 0.0, 0.0 ) );

	if ( ( ( max( nodeVar8.x, max( nodeVar8.y, nodeVar8.z ) ) < 0.0001 ) && ( nodeVar6 < 0.0001 ) ) ) {

		discard;


	}


	if ( ( nodeVar6 < 0.001 ) ) {

		nodeVar9 = ( ( 1.0 - ( nodeVar6 * 0.5 ) ) + ( ( nodeVar6 * nodeVar6 ) / 6.0 ) );

	} else {

		nodeVar9 = ( ( 1.0 - exp( ( - nodeVar6 ) ) ) / max( nodeVar6, 1e-20 ) );

	}

	DiffuseColor = vec4( ( ( ( pow( nodeVar8, vec3( nodeUniform9 ) ) * vec3( nodeVar5 ) ) * vec3( nodeUniform7 ) ) * vec3( nodeVar9 ) ), ( 1.0 - exp( ( - nodeVar6 ) ) ) );
	DiffuseColor.w = ( DiffuseColor.w * nodeUniform10 );
	nodeVar10 = max( vec4( DiffuseColor.xyz, DiffuseColor.w ), 0.0 );
	Output = nodeVar10;

	// result
	fragColor = nodeVar10;

}
