#version 300 es

// Three.js r184 - Node System


// extensions


// precision

precision highp float;
precision highp int;
precision highp sampler2D;
precision highp sampler3D;
precision highp samplerCube;
precision highp sampler2DArray;

precision highp usampler2D;
precision highp usampler3D;
precision highp usamplerCube;
precision highp usampler2DArray;

precision highp isampler2D;
precision highp isampler3D;
precision highp isamplerCube;
precision highp isampler2DArray;

precision highp sampler2DShadow;
precision highp sampler2DArrayShadow;
precision highp samplerCubeShadow;


// structs

layout( location = 0 ) out vec4 fragColor;



// uniforms

layout( std140 ) uniform object {
	mat4 nodeUniform2;
	float nodeUniform3;
	vec2 nodeUniform4;
	float nodeUniform5;
	float nodeUniform6;
	float nodeUniform7;
	float nodeUniform8;
	float nodeUniform9;
	float nodeUniform10;
	float nodeUniform11;
	float nodeUniform12;
};


// varyings
in float nodeVarying0;
in float nodeVarying1;
flat  in float nodeVarying2;
in float nodeVarying3;
in vec3 nodeVarying4;
flat  in float nodeVarying5;
flat  in float nodeVarying6;
in float nodeVarying7;
in float nodeVarying8;
in float nodeVarying9;
in float nodeVarying10;


// vars
vec4 DiffuseColor;
float nodeVar6;
float nodeVar8;
float nodeVar9;
float nodeVar13;
vec3 nodeVar14;
float nodeVar15;
float nodeVar22;
float nodeVar23;
vec4 Output;
vec4 nodeVar24;

// codes


void main() {

	// flow
	// code

	nodeVar6 = abs( nodeVarying0 );

	if ( ( nodeVar6 >= 1.0 ) ) {

		discard;
		

	}

	nodeVar8 = ( nodeVarying1 * nodeVarying2 );
	nodeVar9 = ( ( 1.0 - nodeVarying1 ) * nodeVarying2 );
	nodeVar13 = clamp( ( min( nodeVar8, nodeVar9 ) / nodeVarying3 ), 0.0, 1.0 );
	nodeVar14 = max( ( ( nodeVarying4 * vec3( nodeUniform5 ) ) + vec3( nodeUniform6 ) ), vec3( 0.0, 0.0, 0.0 ) );

	if ( ( max( nodeVar14.x, max( nodeVar14.y, nodeVar14.z ) ) < 0.0001 ) ) {

		discard;
		

	}


	if ( ( nodeVarying3 > 1e-20 ) ) {

		nodeVar15 = nodeVar13;

	} else {

		nodeVar15 = 1.0;

	}


	if ( ( nodeVarying10 >= 0.0 ) ) {

		nodeVar22 = 0.0;

	} else {

		nodeVar23 = max( nodeUniform10, 1e-20 );
		nodeVar22 = smoothstep( nodeVar23, ( nodeVar23 * 2.0 ), ( - nodeVarying10 ) );

	}

	DiffuseColor = vec4( pow( nodeVar14, vec3( nodeUniform7 ) ), ( ( ( ( ( ( mix( ( 0.5 + ( nodeVar15 * 0.5 ) ), 1.0, mix( nodeVarying5, nodeVarying6, step( nodeVar8, nodeVar9 ) ) ) * ( max( ( exp( ( pow( nodeVar6, pow( 2.0, ( ( nodeVarying7 * 6.0 ) - 2.0 ) ) ) * -4.6051702 ) ) - 0.01 ), 0.0 ) * 1.0101010101010102 ) ) * ( 1.0 - smoothstep( ( 1.0 - ( 1.0 / max( nodeVarying8, 1.5 ) ) ), 1.0, nodeVar6 ) ) ) * min( ( nodeVarying8 / 1.5 ), 1.0 ) ) * nodeVarying9 ) * nodeVar22 ) * nodeUniform11 ) );
	DiffuseColor.w = ( DiffuseColor.w * nodeUniform12 );
	nodeVar24 = max( vec4( DiffuseColor.xyz, DiffuseColor.w ), 0.0 );
	Output = nodeVar24;

	// result
	fragColor = nodeVar24;

}
