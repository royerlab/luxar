#version 300 es

// Three.js r185 - Node System


// extensions


// precision

precision highp float;
precision highp int;
precision highp sampler2D;
precision highp sampler3D;
precision highp samplerCube;
precision highp sampler2DArray;

precision highp usampler2D;
precision highp usampler3D;
precision highp usamplerCube;
precision highp usampler2DArray;

precision highp isampler2D;
precision highp isampler3D;
precision highp isamplerCube;
precision highp isampler2DArray;

precision highp sampler2DShadow;
precision highp sampler2DArrayShadow;
precision highp samplerCubeShadow;


// structs

layout( location = 0 ) out vec4 fragColor;



// uniforms

layout( std140 ) uniform object {
	float nodeUniform0;
	mat3 nodeUniform2;
	vec2 nodeUniform3;
	uint nodeUniform4;
	float nodeUniform5;
	float nodeUniform6;
	float nodeUniform7;
	float nodeUniform8;
	float nodeUniform9;
	float nodeUniform10;
	float nodeUniform11;
	float nodeUniform12;
	float nodeUniform13;
	float nodeUniform14;
	float nodeUniform15;
	float nodeUniform16;
	mat4 nodeUniform18;
	mat3 nodeUniform19;
};
uniform sampler2D nodeUniform1;

// varyings
in vec3 nodeVarying0;
in float nodeVarying1;
in vec3 nodeVarying2;
in vec3 nodeVarying3;
in vec2 nodeVarying4;


// vars
vec4 DiffuseColor;
vec2 nodeVar0;
vec2 nodeVar1;
bool nodeVar2;
vec2 nodeVar3;
float nodeVar4;
bool nodeVar5;
vec3 nodeVar7;
vec3 nodeVar8;
vec3 nodeVar9;
float nodeVar11;
float nodeVar12;
vec3 nodeVar13;
vec3 nodeVar14;
vec3 nodeVar15;
vec3 nodeVar16;
float nodeVar17;
vec3 nodeVar19;
vec3 nodeVar20;
float nodeVar21;
float nodeVar22;
float nodeVar23;
float nodeVar24;
float nodeVar25;
vec4 Output;
vec4 nodeVar26;

// codes


void main() {

	// flow
	// code


	if ( ( nodeUniform0 != 0.0 ) ) {

		nodeVar0 = ( nodeUniform2 * vec3( ( vec2( gl_FragCoord.xy.x, nodeUniform3.y - gl_FragCoord.xy.y ) / nodeUniform3 ), 1.0 ) ).xy;
		nodeVar2 = bool( nodeUniform4 );

		if ( nodeVar2 ) {

			nodeVar3 = nodeVar0;
			nodeVar1 = vec2( nodeVar3.x, 1.0 - nodeVar3.y );

		} else {

			nodeVar1 = nodeVar0;

		}

		nodeVar4 = texture( nodeUniform1, nodeVar1 ).x;
		nodeVar5 = ( ( nodeVar4 < 1.0 ) && ( ( ( ( nodeVarying4.x / nodeVarying4.y ) * 0.5 ) + 0.5 ) < nodeVar4 ) );

		if ( ( ( nodeUniform0 == 1.0 ) && nodeVar5 ) ) {

			discard;


		}


		if ( ( ( nodeUniform0 == 2.0 ) && ( ! nodeVar5 ) ) ) {

			discard;


		}



	}

	nodeVar7 = normalize( cross( dFdx( nodeVarying2 ), dFdy( nodeVarying2 ) ) );

	if ( ( nodeVar7.z < 0.0 ) ) {

		nodeVar8 = ( - nodeVar7 );

	} else {

		nodeVar8 = nodeVar7;

	}

	nodeVar9 = nodeVar8;
	nodeVar11 = dot( nodeVarying3, nodeVarying3 );

	if ( gl_FrontFacing ) {

		nodeVar12 = 1.0;

	} else {

		nodeVar12 = -1.0;

	}

	nodeVar13 = ( ( nodeVarying3 * vec3( inversesqrt( max( nodeVar11, 1e-12 ) ) ) ) * vec3( nodeVar12 ) );

	if ( ( ( nodeVar11 >= 1e-12 ) && ( nodeVar11 < 1e30 ) ) ) {

		nodeVar14 = nodeVar13;

	} else {

		nodeVar14 = nodeVar9;

	}

	nodeVar15 = nodeVar14;
	nodeVar16 = normalize( vec3( -0.35, 0.55, 0.75 ) );
	nodeVar17 = mix( nodeUniform5, 1.0, pow( clamp( ( ( dot( nodeVar15, nodeVar16 ) * 0.5 ) + 0.5 ), 0.0, 1.0 ), nodeUniform6 ) );
	nodeVar19 = max( ( ( nodeVarying0 * vec3( nodeUniform7 ) ) + vec3( nodeUniform8 ) ), vec3( 0.0, 0.0, 0.0 ) );
	nodeVar20 = ( ( pow( nodeVar19, vec3( nodeUniform9 ) ) * vec3( nodeVar17 ) ) + vec3( ( nodeUniform10 * pow( max( dot( nodeVar15, normalize( ( nodeVar16 + vec3( 0.0, 0.0, 1.0 ) ) ) ), 0.0 ), nodeUniform11 ) ) ) );
	nodeVar21 = ( nodeVarying1 * nodeUniform12 );

	if ( ( int( nodeUniform13 ) == 1 ) ) {

		nodeVar22 = 1.0;

	} else {


		if ( ( nodeVarying2.z >= 0.0 ) ) {

			nodeVar23 = 0.0;

		} else {

			nodeVar24 = max( nodeUniform14, 1e-20 );
			nodeVar23 = smoothstep( nodeVar24, ( nodeVar24 * 2.0 ), ( - nodeVarying2.z ) );

		}

		nodeVar22 = nodeVar23;

	}

	nodeVar25 = nodeVar22;

	if ( ( nodeVar25 < 0.01 ) ) {

		discard;


	}


	if ( ( nodeVar21 < nodeUniform15 ) ) {

		discard;


	}

	DiffuseColor = vec4( ( nodeVar20 * vec3( nodeVar25 ) ), 1.0 );
	DiffuseColor.w = ( DiffuseColor.w * nodeUniform16 );
	nodeVar26 = max( vec4( DiffuseColor.xyz, DiffuseColor.w ), 0.0 );
	Output = nodeVar26;

	// result
	fragColor = nodeVar26;

}
