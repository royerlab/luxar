#version 300 es

// Three.js r185 - Node System


// extensions


// precision

precision highp float;
precision highp int;
precision highp sampler2D;
precision highp sampler3D;
precision highp samplerCube;
precision highp sampler2DArray;

precision highp usampler2D;
precision highp usampler3D;
precision highp usamplerCube;
precision highp usampler2DArray;

precision highp isampler2D;
precision highp isampler3D;
precision highp isamplerCube;
precision highp isampler2DArray;

precision highp sampler2DShadow;
precision highp sampler2DArrayShadow;
precision highp samplerCubeShadow;


// structs

layout( location = 0 ) out vec4 fragColor;



// uniforms

layout( std140 ) uniform object {
	float nodeUniform0;
	mat3 nodeUniform2;
	vec2 nodeUniform3;
	uint nodeUniform4;
	float nodeUniform5;
	float nodeUniform6;
	float nodeUniform7;
	float nodeUniform8;
	float nodeUniform9;
	float nodeUniform10;
	float nodeUniform11;
	float nodeUniform12;
	mat4 nodeUniform14;
};
uniform sampler2D nodeUniform1;

// varyings
in vec3 nodeVarying0;
in float nodeVarying1;
in vec3 nodeVarying2;
in vec2 nodeVarying3;


// vars
vec4 DiffuseColor;
vec2 nodeVar0;
vec2 nodeVar1;
bool nodeVar2;
vec2 nodeVar3;
float nodeVar4;
bool nodeVar5;
vec3 nodeVar7;
vec3 nodeVar8;
float nodeVar9;
float nodeVar10;
float nodeVar11;
float nodeVar13;
float nodeVar14;
vec4 Output;
vec4 nodeVar15;

// codes


void main() {

	// flow
	// code


	if ( ( nodeUniform0 != 0.0 ) ) {

		nodeVar0 = ( nodeUniform2 * vec3( ( vec2( gl_FragCoord.xy.x, nodeUniform3.y - gl_FragCoord.xy.y ) / nodeUniform3 ), 1.0 ) ).xy;
		nodeVar2 = bool( nodeUniform4 );

		if ( nodeVar2 ) {

			nodeVar3 = nodeVar0;
			nodeVar1 = vec2( nodeVar3.x, 1.0 - nodeVar3.y );

		} else {

			nodeVar1 = nodeVar0;

		}

		nodeVar4 = texture( nodeUniform1, nodeVar1 ).x;
		nodeVar5 = ( ( nodeVar4 < 1.0 ) && ( ( ( ( nodeVarying3.x / nodeVarying3.y ) * 0.5 ) + 0.5 ) < nodeVar4 ) );

		if ( ( ( nodeUniform0 == 1.0 ) && nodeVar5 ) ) {

			discard;


		}


		if ( ( ( nodeUniform0 == 2.0 ) && ( ! nodeVar5 ) ) ) {

			discard;


		}



	}

	nodeVar7 = max( ( ( nodeVarying0 * vec3( nodeUniform5 ) ) + vec3( nodeUniform6 ) ), vec3( 0.0, 0.0, 0.0 ) );
	nodeVar8 = pow( nodeVar7, vec3( nodeUniform7 ) );
	nodeVar9 = ( nodeVarying1 * nodeUniform8 );

	if ( ( int( nodeUniform9 ) == 1 ) ) {

		nodeVar10 = 1.0;

	} else {


		if ( ( nodeVarying2.z >= 0.0 ) ) {

			nodeVar11 = 0.0;

		} else {

			nodeVar13 = max( nodeUniform10, 1e-20 );
			nodeVar11 = smoothstep( nodeVar13, ( nodeVar13 * 2.0 ), ( - nodeVarying2.z ) );

		}

		nodeVar10 = nodeVar11;

	}

	nodeVar14 = nodeVar10;

	if ( ( nodeVar14 < 0.01 ) ) {

		discard;


	}


	if ( ( nodeVar9 < nodeUniform11 ) ) {

		discard;


	}

	DiffuseColor = vec4( ( nodeVar8 * vec3( nodeVar14 ) ), 1.0 );
	DiffuseColor.w = ( DiffuseColor.w * nodeUniform12 );
	nodeVar15 = max( vec4( DiffuseColor.xyz, DiffuseColor.w ), 0.0 );
	Output = nodeVar15;

	// result
	fragColor = nodeVar15;

}
