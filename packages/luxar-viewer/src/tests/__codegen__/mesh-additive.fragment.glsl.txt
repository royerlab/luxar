#version 300 es

// Three.js r185 - Node System


// extensions


// precision

precision highp float;
precision highp int;
precision highp sampler2D;
precision highp sampler3D;
precision highp samplerCube;
precision highp sampler2DArray;

precision highp usampler2D;
precision highp usampler3D;
precision highp usamplerCube;
precision highp usampler2DArray;

precision highp isampler2D;
precision highp isampler3D;
precision highp isamplerCube;
precision highp isampler2DArray;

precision highp sampler2DShadow;
precision highp sampler2DArrayShadow;
precision highp samplerCubeShadow;


// structs

layout( location = 0 ) out vec4 fragColor;



// uniforms

layout( std140 ) uniform object {
	float nodeUniform0;
	float nodeUniform1;
	float nodeUniform2;
	float nodeUniform3;
	float nodeUniform4;
	float nodeUniform5;
	float nodeUniform6;
	float nodeUniform7;
	float nodeUniform8;
	mat4 nodeUniform10;
	mat3 nodeUniform11;
};


// varyings
in vec3 nodeVarying0;
in float nodeVarying1;
in vec3 nodeVarying2;
in vec3 nodeVarying3;


// vars
vec4 DiffuseColor;
vec3 nodeVar1;
vec3 nodeVar2;
vec3 nodeVar3;
float nodeVar5;
float nodeVar6;
vec3 nodeVar7;
vec3 nodeVar8;
vec3 nodeVar9;
float nodeVar10;
vec3 nodeVar12;
vec3 nodeVar13;
float nodeVar14;
float nodeVar15;
float nodeVar16;
float nodeVar17;
float nodeVar18;
float nodeVar19;
vec4 Output;
vec4 nodeVar20;

// codes


void main() {

	// flow
	// code

	nodeVar1 = normalize( cross( dFdx( nodeVarying2 ), dFdy( nodeVarying2 ) ) );

	if ( ( nodeVar1.z < 0.0 ) ) {

		nodeVar2 = ( - nodeVar1 );

	} else {

		nodeVar2 = nodeVar1;

	}

	nodeVar3 = nodeVar2;
	nodeVar5 = dot( nodeVarying3, nodeVarying3 );

	if ( gl_FrontFacing ) {

		nodeVar6 = 1.0;

	} else {

		nodeVar6 = -1.0;

	}

	nodeVar7 = ( ( nodeVarying3 * vec3( inversesqrt( max( nodeVar5, 1e-12 ) ) ) ) * vec3( nodeVar6 ) );

	if ( ( ( nodeVar5 >= 1e-12 ) && ( nodeVar5 < 1e30 ) ) ) {

		nodeVar8 = nodeVar7;

	} else {

		nodeVar8 = nodeVar3;

	}

	nodeVar9 = nodeVar8;
	nodeVar10 = mix( nodeUniform0, 1.0, pow( clamp( ( ( nodeVar9.z * 0.5 ) + 0.5 ), 0.0, 1.0 ), nodeUniform1 ) );
	nodeVar12 = max( ( ( nodeVarying0 * vec3( nodeUniform2 ) ) + vec3( nodeUniform3 ) ), vec3( 0.0, 0.0, 0.0 ) );
	nodeVar13 = ( pow( nodeVar12, vec3( nodeUniform4 ) ) * vec3( nodeVar10 ) );
	nodeVar14 = ( nodeVarying1 * nodeUniform5 );

	if ( ( int( nodeUniform6 ) == 1 ) ) {

		nodeVar15 = 1.0;

	} else {


		if ( ( nodeVarying2.z >= 0.0 ) ) {

			nodeVar16 = 0.0;

		} else {

			nodeVar17 = max( nodeUniform7, 1e-20 );
			nodeVar16 = smoothstep( nodeVar17, ( nodeVar17 * 2.0 ), ( - nodeVarying2.z ) );

		}

		nodeVar15 = nodeVar16;

	}

	nodeVar18 = nodeVar15;

	if ( ( nodeVar18 < 0.01 ) ) {

		discard;


	}

	nodeVar19 = ( nodeVar14 * nodeVar18 );
	DiffuseColor = vec4( nodeVar13, nodeVar19 );
	DiffuseColor.w = ( DiffuseColor.w * nodeUniform8 );
	nodeVar20 = max( vec4( DiffuseColor.xyz, DiffuseColor.w ), 0.0 );
	Output = nodeVar20;

	// result
	fragColor = nodeVar20;

}
