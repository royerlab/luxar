#version 300 es

// Three.js r185 - Node System


// extensions


// precision

precision highp float;
precision highp int;
precision highp sampler2D;
precision highp sampler3D;
precision highp samplerCube;
precision highp sampler2DArray;

precision highp usampler2D;
precision highp usampler3D;
precision highp usamplerCube;
precision highp usampler2DArray;

precision highp isampler2D;
precision highp isampler3D;
precision highp isamplerCube;
precision highp isampler2DArray;

precision highp sampler2DShadow;
precision highp sampler2DArrayShadow;
precision highp samplerCubeShadow;


// structs

layout( location = 0 ) out vec4 fragColor;



// uniforms

layout( std140 ) uniform object {
	float nodeUniform0;
	mat3 nodeUniform2;
	vec2 nodeUniform3;
	uint nodeUniform4;
	float nodeUniform5;
	float nodeUniform6;
	float nodeUniform7;
	float nodeUniform8;
	float nodeUniform9;
	float nodeUniform10;
	float nodeUniform11;
	float nodeUniform12;
	mat3 nodeUniform14;
	uint nodeUniform15;
	mat3 nodeUniform16;
	uint nodeUniform17;
	mat3 nodeUniform18;
	uint nodeUniform19;
	float nodeUniform20;
	mat4 nodeUniform22;
	float nodeUniform24;
	float nodeUniform25;
	float nodeUniform26;
	mat3 nodeUniform28;
	float nodeUniform29;
	float nodeUniform30;
	float nodeUniform31;
	uint nodeUniform32;
	float nodeUniform33;
	float nodeUniform34;
	float nodeUniform35;
	vec2 nodeUniform36;
};
uniform sampler2D nodeUniform1;

// varyings
in vec2 nodeVarying0;
in float nodeVarying1;
in float nodeVarying2;
in vec3 nodeVarying3;
in float nodeVarying4;
in float nodeVarying5;
in float nodeVarying6;
in vec2 nodeVarying7;


// vars
vec4 DiffuseColor;
vec2 nodeVar0;
vec2 nodeVar1;
bool nodeVar2;
vec2 nodeVar3;
float nodeVar4;
bool nodeVar5;
float nodeVar7;
vec3 nodeVar9;
float nodeVar10;
float nodeVar11;
float nodeVar12;
float nodeVar13;
float nodeVar14;
vec4 Output;
vec4 nodeVar15;

// codes


void main() {

	// flow
	// code


	if ( ( nodeUniform0 != 0.0 ) ) {

		nodeVar0 = ( nodeUniform2 * vec3( ( vec2( gl_FragCoord.xy.x, nodeUniform3.y - gl_FragCoord.xy.y ) / nodeUniform3 ), 1.0 ) ).xy;
		nodeVar2 = bool( nodeUniform4 );

		if ( nodeVar2 ) {

			nodeVar3 = nodeVar0;
			nodeVar1 = vec2( nodeVar3.x, 1.0 - nodeVar3.y );

		} else {

			nodeVar1 = nodeVar0;

		}

		nodeVar4 = texture( nodeUniform1, nodeVar1 ).x;
		nodeVar5 = ( ( nodeVar4 < 1.0 ) && ( ( ( ( nodeVarying7.x / nodeVarying7.y ) * 0.5 ) + 0.5 ) < nodeVar4 ) );

		if ( ( ( nodeUniform0 == 1.0 ) && nodeVar5 ) ) {

			discard;


		}


		if ( ( ( nodeUniform0 == 2.0 ) && ( ! nodeVar5 ) ) ) {

			discard;


		}



	}


	if ( ( nodeVarying1 <= 0.0 ) ) {

		discard;


	}

	nodeVar7 = dot( ( nodeVarying0 - vec2( 0.5 ) ), ( nodeVarying0 - vec2( 0.5 ) ) );

	if ( ( nodeVar7 > 0.25 ) ) {

		discard;


	}

	nodeVar9 = max( ( ( nodeVarying3 * vec3( nodeUniform5 ) ) + vec3( nodeUniform6 ) ), vec3( 0.0, 0.0, 0.0 ) );
	nodeVar10 = min( ( nodeVarying4 / ( max( nodeUniform8, 1.0 ) * 1.5 ) ), 1.0 );
	nodeVar11 = ( ( ( ( max( ( exp( ( pow( sqrt( ( nodeVar7 * 4.0 ) ), nodeVarying2 ) * -4.6051702 ) ) - 0.01 ), 0.0 ) * 1.0101010101010102 ) * nodeUniform7 ) * ( nodeVar10 * nodeVar10 ) ) * nodeVarying5 );
	nodeVar12 = ( nodeVar11 * mix( 1.0, ( - log( ( 1.0 - min( nodeVarying6, 0.998046875 ) ) ) ), nodeUniform9 ) );
	nodeVar13 = ( nodeUniform10 * nodeVar12 );

	if ( ( ( max( nodeVar9.x, max( nodeVar9.y, nodeVar9.z ) ) < 0.0001 ) && ( nodeVar13 < 0.0001 ) ) ) {

		discard;


	}


	if ( ( nodeVar13 < 0.001 ) ) {

		nodeVar14 = ( ( 1.0 - ( nodeVar13 * 0.5 ) ) + ( ( nodeVar13 * nodeVar13 ) / 6.0 ) );

	} else {

		nodeVar14 = ( ( 1.0 - exp( ( - nodeVar13 ) ) ) / max( nodeVar13, 1e-20 ) );

	}

	DiffuseColor = vec4( ( ( nodeVar9 * vec3( nodeVar12 ) ) * vec3( nodeVar14 ) ), ( 1.0 - exp( ( - nodeVar13 ) ) ) );
	DiffuseColor.w = ( DiffuseColor.w * nodeUniform11 );
	nodeVar15 = max( vec4( DiffuseColor.xyz, DiffuseColor.w ), 0.0 );
	Output = nodeVar15;

	// result
	fragColor = nodeVar15;

}
