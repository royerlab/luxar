#version 300 es

// Three.js r184 - Node System


// extensions


// precision

precision highp float;
precision highp int;
precision highp sampler2D;
precision highp sampler3D;
precision highp samplerCube;
precision highp sampler2DArray;

precision highp usampler2D;
precision highp usampler3D;
precision highp usamplerCube;
precision highp usampler2DArray;

precision highp isampler2D;
precision highp isampler3D;
precision highp isamplerCube;
precision highp isampler2DArray;

precision highp sampler2DShadow;
precision highp sampler2DArrayShadow;
precision highp samplerCubeShadow;


// structs

layout( location = 0 ) out vec4 fragColor;



// uniforms

layout( std140 ) uniform object {
	mat4 nodeUniform2;
	vec2 nodeUniform3;
	float nodeUniform4;
	float nodeUniform5;
	float nodeUniform6;
	float nodeUniform7;
	float nodeUniform8;
	float nodeUniform9;
	float nodeUniform10;
};


// varyings
in float nodeVarying0;
in float nodeVarying1;
flat  in float nodeVarying2;
in float nodeVarying3;
in vec3 nodeVarying4;
flat  in float nodeVarying5;
flat  in float nodeVarying6;
in float nodeVarying7;
in float nodeVarying8;
in float nodeVarying9;


// vars
vec4 DiffuseColor;
float nodeVar16;
float nodeVar17;
float nodeVar18;
float nodeVar19;
vec3 nodeVar20;
float nodeVar21;
vec4 Output;
vec4 nodeVar26;

// codes


void main() {

	// flow
	// code

	nodeVar16 = abs( nodeVarying0 );

	if ( ( nodeVar16 >= 1.0 ) ) {

		discard;
		

	}

	nodeVar17 = ( nodeVarying1 * nodeVarying2 );
	nodeVar18 = ( ( 1.0 - nodeVarying1 ) * nodeVarying2 );
	nodeVar19 = clamp( ( min( nodeVar17, nodeVar18 ) / nodeVarying3 ), 0.0, 1.0 );
	nodeVar20 = max( ( ( nodeVarying4 * vec3( nodeUniform6 ) ) + vec3( nodeUniform7 ) ), vec3( 0.0, 0.0, 0.0 ) );

	if ( ( max( nodeVar20.x, max( nodeVar20.y, nodeVar20.z ) ) < 0.0001 ) ) {

		discard;
		

	}


	if ( ( nodeVarying3 > 0.0001 ) ) {

		nodeVar21 = nodeVar19;

	} else {

		nodeVar21 = 1.0;

	}

	DiffuseColor = vec4( nodeVar20, ( ( ( ( ( mix( ( 0.5 + ( nodeVar21 * 0.5 ) ), 1.0, mix( nodeVarying5, nodeVarying6, step( nodeVar17, nodeVar18 ) ) ) * pow( max( ( 1.0 - ( nodeVar16 * nodeVar16 ) ), 0.0 ), max( nodeVarying7, 0.0001 ) ) ) * ( 1.0 - smoothstep( ( 1.0 - ( 1.0 / max( nodeVarying8, 1.5 ) ) ), 1.0, nodeVar16 ) ) ) * min( ( nodeVarying8 / 1.5 ), 1.0 ) ) * nodeVarying9 ) * nodeUniform8 ) );
	DiffuseColor.w = ( DiffuseColor.w * nodeUniform9 );
	nodeVar26 = max( vec4( DiffuseColor.xyz, DiffuseColor.w ), 0.0 );
	Output = nodeVar26;

	// result
	fragColor = nodeVar26;

}
