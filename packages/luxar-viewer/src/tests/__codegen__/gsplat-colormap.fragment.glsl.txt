#version 300 es

// Three.js r185 - Node System


// extensions


// precision

precision highp float;
precision highp int;
precision highp sampler2D;
precision highp sampler3D;
precision highp samplerCube;
precision highp sampler2DArray;

precision highp usampler2D;
precision highp usampler3D;
precision highp usamplerCube;
precision highp usampler2DArray;

precision highp isampler2D;
precision highp isampler3D;
precision highp isamplerCube;
precision highp isampler2DArray;

precision highp sampler2DShadow;
precision highp sampler2DArrayShadow;
precision highp samplerCubeShadow;


// structs

layout( location = 0 ) out vec4 fragColor;



// uniforms

layout( std140 ) uniform object {
	float nodeUniform0;
	mat3 nodeUniform2;
	vec2 nodeUniform3;
	uint nodeUniform4;
	vec2 nodeUniform5;
	float nodeUniform6;
	float nodeUniform7;
	float nodeUniform8;
	float nodeUniform9;
	float nodeUniform10;
	float nodeUniform11;
	float nodeUniform12;
	float nodeUniform13;
	mat3 nodeUniform15;
	uint nodeUniform16;
	mat3 nodeUniform17;
	uint nodeUniform18;
	mat3 nodeUniform19;
	uint nodeUniform20;
	mat3 nodeUniform21;
	uint nodeUniform22;
	mat4 nodeUniform24;
	float nodeUniform26;
	vec2 nodeUniform27;
	float nodeUniform28;
	float nodeUniform29;
	float nodeUniform30;
	float nodeUniform31;
	float nodeUniform32;
	mat3 nodeUniform34;
	float nodeUniform35;
	float nodeUniform36;
	float nodeUniform37;
	uint nodeUniform38;
	float nodeUniform39;
	float nodeUniform40;
};
uniform sampler2D nodeUniform1;

// varyings
in vec3 nodeVarying0;
in float nodeVarying1;
in float nodeVarying2;
in vec3 nodeVarying3;
in vec2 nodeVarying4;
in vec2 nodeVarying5;


// vars
vec4 DiffuseColor;
vec2 nodeVar0;
vec2 nodeVar1;
bool nodeVar2;
vec2 nodeVar3;
float nodeVar4;
bool nodeVar5;
float nodeVar8;
float nodeVar9;
float nodeVar10;
float nodeVar11;
vec3 nodeVar13;
vec4 Output;
vec4 nodeVar14;

// codes


void main() {

	// flow
	// code


	if ( ( nodeUniform0 != 0.0 ) ) {

		nodeVar0 = ( nodeUniform2 * vec3( ( vec2( gl_FragCoord.xy.x, nodeUniform3.y - gl_FragCoord.xy.y ) / nodeUniform3 ), 1.0 ) ).xy;
		nodeVar2 = bool( nodeUniform4 );

		if ( nodeVar2 ) {

			nodeVar3 = nodeVar0;
			nodeVar1 = vec2( nodeVar3.x, 1.0 - nodeVar3.y );

		} else {

			nodeVar1 = nodeVar0;

		}

		nodeVar4 = texture( nodeUniform1, nodeVar1 ).x;
		nodeVar5 = ( ( nodeVar4 < 1.0 ) && ( ( ( ( nodeVarying5.x / nodeVarying5.y ) * 0.5 ) + 0.5 ) < nodeVar4 ) );

		if ( ( ( nodeUniform0 == 1.0 ) && nodeVar5 ) ) {

			discard;


		}


		if ( ( ( nodeUniform0 == 2.0 ) && ( ! nodeVar5 ) ) ) {

			discard;


		}



	}

	nodeVar8 = ( ( vec2( vec2( gl_FragCoord.xy.x, nodeUniform5.y - gl_FragCoord.xy.y ).x, ( nodeUniform5.y - vec2( gl_FragCoord.xy.x, nodeUniform5.y - gl_FragCoord.xy.y ).y ) ) - nodeVarying4 ).x * nodeVarying3.x );
	nodeVar9 = ( ( ( vec2( vec2( gl_FragCoord.xy.x, nodeUniform5.y - gl_FragCoord.xy.y ).x, ( nodeUniform5.y - vec2( gl_FragCoord.xy.x, nodeUniform5.y - gl_FragCoord.xy.y ).y ) ) - nodeVarying4 ).y - ( nodeVarying3.y * nodeVar8 ) ) * nodeVarying3.z );
	nodeVar10 = ( ( nodeVar8 * nodeVar8 ) + ( nodeVar9 * nodeVar9 ) );

	if ( ( nodeVar10 > nodeUniform6 ) ) {

		discard;


	}

	nodeVar11 = ( ( ( nodeVarying2 * nodeUniform7 ) * max( ( exp( ( nodeVar10 * -0.5 ) ) - nodeUniform8 ), 0.0 ) ) * nodeVarying1 );

	if ( ( ( nodeVar11 * max( nodeUniform9, 1.0 ) ) < 0.0001 ) ) {

		discard;


	}

	nodeVar13 = max( ( ( nodeVarying0 * vec3( nodeUniform9 ) ) + vec3( nodeUniform10 ) ), vec3( 0.0, 0.0, 0.0 ) );

	if ( ( max( nodeVar13.x, max( nodeVar13.y, nodeVar13.z ) ) < 0.0001 ) ) {

		discard;


	}

	DiffuseColor = vec4( ( ( nodeVar13 * vec3( nodeVar11 ) ) * vec3( nodeUniform11 ) ), 1.0 );
	DiffuseColor.w = ( DiffuseColor.w * nodeUniform12 );
	nodeVar14 = max( vec4( DiffuseColor.xyz, DiffuseColor.w ), 0.0 );
	Output = nodeVar14;

	// result
	fragColor = nodeVar14;

}
