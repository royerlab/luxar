#version 300 es

// Three.js r184 - Node System


// extensions


// precision

precision highp float;
precision highp int;
precision highp sampler2D;
precision highp sampler3D;
precision highp samplerCube;
precision highp sampler2DArray;

precision highp usampler2D;
precision highp usampler3D;
precision highp usamplerCube;
precision highp usampler2DArray;

precision highp isampler2D;
precision highp isampler3D;
precision highp isamplerCube;
precision highp isampler2DArray;

precision highp sampler2DShadow;
precision highp sampler2DArrayShadow;
precision highp samplerCubeShadow;


// structs

layout( location = 0 ) out vec4 fragColor;



// uniforms

layout( std140 ) uniform object {
	float nodeUniform0;
	float nodeUniform1;
	mat4 nodeUniform3;
	float nodeUniform4;
	vec2 nodeUniform6;
	float nodeUniform7;
	float nodeUniform8;
	float nodeUniform9;
	float nodeUniform10;
	float nodeUniform11;
	float nodeUniform12;
	float nodeUniform13;
	float nodeUniform14;
	float nodeUniform15;
	mat4 nodeUniform17;
	float nodeUniform18;
};

layout( std140 ) uniform render {
	mat4 cameraViewMatrix;
	mat4 cameraProjectionMatrix;
};


// varyings
in float nodeVarying0;
in float nodeVarying1;
flat  in float nodeVarying2;
in float nodeVarying3;
in vec3 nodeVarying4;
flat  in float nodeVarying5;
flat  in float nodeVarying6;
in float nodeVarying7;
in float nodeVarying8;
in float nodeVarying9;
in float nodeVarying10;
in vec2 nodeVarying11;
in vec3 nodeVarying12;
in vec3 nodeVarying13;


// vars
vec4 DiffuseColor;
float nodeVar19;
float nodeVar20;
float nodeVar21;
float nodeVar22;
vec3 nodeVar23;
float nodeVar24;
float nodeVar25;
vec4 Output;
vec4 nodeVar28;
float nodeVar29;
float nodeVar30;
float nodeVar31;
float nodeVar32;
float nodeVar33;
mat4 modelViewMatrix;
vec4 nodeVar34;
vec4 nodeVar35;
float nodeVar36;
float nodeVar37;

// codes


void main() {

	// flow
	// code

	nodeVar19 = abs( nodeVarying0 );

	if ( ( nodeVar19 >= 1.0 ) ) {

		discard;
		

	}

	nodeVar20 = ( nodeVarying1 * nodeVarying2 );
	nodeVar21 = ( ( 1.0 - nodeVarying1 ) * nodeVarying2 );
	nodeVar22 = clamp( ( min( nodeVar20, nodeVar21 ) / nodeVarying3 ), 0.0, 1.0 );
	nodeVar23 = max( ( ( nodeVarying4 * vec3( nodeUniform9 ) ) + vec3( nodeUniform10 ) ), vec3( 0.0, 0.0, 0.0 ) );

	if ( ( max( nodeVar23.x, max( nodeVar23.y, nodeVar23.z ) ) < 0.0001 ) ) {

		discard;
		

	}


	if ( ( nodeVarying3 > 0.0001 ) ) {

		nodeVar24 = nodeVar22;

	} else {

		nodeVar24 = 1.0;

	}

	nodeVar25 = max( ( 1.0 - ( nodeVar19 * nodeVar19 ) ), 0.0 );
	DiffuseColor = vec4( pow( nodeVar23, vec3( nodeUniform11 ) ), ( ( ( ( ( mix( ( 0.5 + ( nodeVar24 * 0.5 ) ), 1.0, mix( nodeVarying5, nodeVarying6, step( nodeVar20, nodeVar21 ) ) ) * ( nodeVar25 * nodeVar25 ) ) * ( 1.0 - smoothstep( ( 1.0 - ( 1.0 / max( nodeVarying7, 1.5 ) ) ), 1.0, nodeVar19 ) ) ) * min( ( nodeVarying7 / 1.5 ), 1.0 ) ) * nodeVarying8 ) * nodeUniform12 ) );
	DiffuseColor.w = ( DiffuseColor.w * nodeUniform13 );
	nodeVar28 = max( vec4( DiffuseColor.xyz, DiffuseColor.w ), 0.0 );
	Output = nodeVar28;

	if ( ( ( ( nodeVarying9 < 1e30 ) && ( nodeVarying9 > -1e30 ) ) && ( nodeVarying9 >= 0.0 ) ) ) {

		nodeVar29 = nodeVarying9;

	} else {

		nodeVar29 = 0.0;

	}


	if ( ( ( ( nodeVarying10 < 1e30 ) && ( nodeVarying10 > -1e30 ) ) && ( nodeVarying10 >= 0.0 ) ) ) {

		nodeVar30 = nodeVarying10;

	} else {

		nodeVar30 = 0.0;

	}

	nodeVar31 = ( ( nodeVarying11.x * 0.5 ) + 0.5 );
	nodeVar32 = mix( nodeVar29, nodeVar30, nodeVar31 );
	nodeVar33 = ( nodeVar32 * nodeUniform14 );
	modelViewMatrix = ( cameraViewMatrix * nodeUniform17 );
	nodeVar34 = ( modelViewMatrix * vec4( nodeVarying12, 1.0 ) );
	nodeVar35 = ( modelViewMatrix * vec4( nodeVarying13, 1.0 ) );
	nodeVar36 = max( nodeUniform18, 0.0001 );
	nodeVar37 = ( ( nodeVar32 * nodeUniform15 ) / max( ( - mix( nodeVar34, nodeVar35, nodeVar31 ).z ), nodeVar36 ) );

	// result
	fragColor = nodeVar28;

}
