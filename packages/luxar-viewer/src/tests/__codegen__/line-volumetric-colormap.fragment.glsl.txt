#version 300 es

// Three.js r185 - Node System


// extensions


// precision

precision highp float;
precision highp int;
precision highp sampler2D;
precision highp sampler3D;
precision highp samplerCube;
precision highp sampler2DArray;

precision highp usampler2D;
precision highp usampler3D;
precision highp usamplerCube;
precision highp usampler2DArray;

precision highp isampler2D;
precision highp isampler3D;
precision highp isamplerCube;
precision highp isampler2DArray;

precision highp sampler2DShadow;
precision highp sampler2DArrayShadow;
precision highp samplerCubeShadow;


// structs

layout( location = 0 ) out vec4 fragColor;



// uniforms

layout( std140 ) uniform object {
	mat4 nodeUniform1;
	float nodeUniform2;
	float nodeUniform3;
	float nodeUniform4;
	float nodeUniform5;
	float nodeUniform6;
	float nodeUniform7;
	float nodeUniform8;
	float nodeUniform9;
	mat3 nodeUniform11;
	uint nodeUniform12;
	mat3 nodeUniform13;
	uint nodeUniform14;
	mat3 nodeUniform15;
	uint nodeUniform16;
	mat3 nodeUniform17;
	uint nodeUniform18;
	mat3 nodeUniform19;
	uint nodeUniform20;
	mat3 nodeUniform21;
	uint nodeUniform22;
	vec2 nodeUniform24;
	float nodeUniform25;
	float nodeUniform26;
	float nodeUniform27;
	mat3 nodeUniform28;
	uint nodeUniform29;
	mat3 nodeUniform30;
	uint nodeUniform31;
	mat3 nodeUniform32;
	uint nodeUniform33;
	mat3 nodeUniform34;
	uint nodeUniform35;
	mat3 nodeUniform37;
	float nodeUniform38;
	float nodeUniform39;
	float nodeUniform40;
	uint nodeUniform41;
};


// varyings
in vec3 nodeVarying0;
in float nodeVarying1;
in float nodeVarying2;
in float nodeVarying3;
flat  in float nodeVarying4;
in float nodeVarying5;
in float nodeVarying6;
in float nodeVarying7;
flat  in float nodeVarying8;
flat  in float nodeVarying9;
in float nodeVarying10;


// vars
vec4 DiffuseColor;
float nodeVar0;
float nodeVar1;
float nodeVar2;
vec3 nodeVar4;
float nodeVar5;
float nodeVar6;
float nodeVar7;
float nodeVar8;
float nodeVar9;
float nodeVar10;
float nodeVar11;
vec4 Output;
vec4 nodeVar12;

// codes


void main() {

	// flow
	// code

	nodeVar0 = abs( nodeVarying2 );

	if ( ( nodeVar0 >= 1.0 ) ) {

		discard;


	}

	nodeVar1 = clamp( ( ( nodeVarying3 * nodeVarying4 ) / nodeVarying5 ), 0.0, 1.0 );
	nodeVar2 = clamp( ( ( ( 1.0 - nodeVarying3 ) * nodeVarying4 ) / nodeVarying5 ), 0.0, 1.0 );
	nodeVar4 = max( ( ( nodeVarying0 * vec3( nodeUniform2 ) ) + vec3( nodeUniform3 ) ), vec3( 0.0, 0.0, 0.0 ) );

	if ( ( nodeVarying5 > 1e-20 ) ) {

		nodeVar5 = nodeVar1;

	} else {

		nodeVar5 = 1.0;

	}


	if ( ( nodeVarying5 > 1e-20 ) ) {

		nodeVar6 = nodeVar2;

	} else {

		nodeVar6 = 1.0;

	}

	nodeVar7 = ( max( nodeUniform4, 1.0 ) * 1.5 );
	nodeVar8 = ( ( ( ( ( ( min( mix( ( 0.5 + ( nodeVar5 * 0.5 ) ), 1.0, nodeVarying8 ), mix( ( 0.5 + ( nodeVar6 * 0.5 ) ), 1.0, nodeVarying9 ) ) * ( max( ( exp( ( pow( nodeVar0, pow( 2.0, ( ( nodeVarying1 * 6.0 ) - 2.0 ) ) ) * -4.6051702 ) ) - 0.01 ), 0.0 ) * 1.0101010101010102 ) ) * ( 1.0 - smoothstep( ( 1.0 - ( 1.0 / max( nodeVarying6, nodeVar7 ) ) ), 1.0, nodeVar0 ) ) ) * min( ( nodeVarying6 / nodeVar7 ), 1.0 ) ) * nodeVarying7 ) * 1.0 ) * nodeUniform5 );
	nodeVar9 = ( nodeVar8 * mix( 1.0, ( - log( ( 1.0 - min( nodeVarying10, 0.998046875 ) ) ) ), nodeUniform6 ) );
	nodeVar10 = ( nodeUniform7 * nodeVar9 );

	if ( ( ( max( nodeVar4.x, max( nodeVar4.y, nodeVar4.z ) ) < 0.0001 ) && ( nodeVar10 < 0.0001 ) ) ) {

		discard;


	}


	if ( ( nodeVar10 < 0.001 ) ) {

		nodeVar11 = ( ( 1.0 - ( nodeVar10 * 0.5 ) ) + ( ( nodeVar10 * nodeVar10 ) / 6.0 ) );

	} else {

		nodeVar11 = ( ( 1.0 - exp( ( - nodeVar10 ) ) ) / max( nodeVar10, 1e-20 ) );

	}

	DiffuseColor = vec4( ( ( nodeVar4 * vec3( nodeVar9 ) ) * vec3( nodeVar11 ) ), ( 1.0 - exp( ( - nodeVar10 ) ) ) );
	DiffuseColor.w = ( DiffuseColor.w * nodeUniform8 );
	nodeVar12 = max( vec4( DiffuseColor.xyz, DiffuseColor.w ), 0.0 );
	Output = nodeVar12;

	// result
	fragColor = nodeVar12;

}
