#version 300 es

// Three.js r185 - Node System


// extensions


// precision

precision highp float;
precision highp int;
precision highp sampler2D;
precision highp sampler3D;
precision highp samplerCube;
precision highp sampler2DArray;

precision highp usampler2D;
precision highp usampler3D;
precision highp usamplerCube;
precision highp usampler2DArray;

precision highp isampler2D;
precision highp isampler3D;
precision highp isamplerCube;
precision highp isampler2DArray;

precision highp sampler2DShadow;
precision highp sampler2DArrayShadow;
precision highp samplerCubeShadow;


// structs





// uniforms

layout( std140 ) uniform render {
	mat4 cameraViewMatrix;
	mat4 cameraProjectionMatrix;
};

layout( std140 ) uniform object {
	float nodeUniform0;
	mat3 nodeUniform2;
	vec2 nodeUniform3;
	uint nodeUniform4;
	mat3 nodeUniform6;
	uint nodeUniform7;
	float nodeUniform8;
	float nodeUniform9;
	float nodeUniform10;
	float nodeUniform11;
	float nodeUniform12;
	float nodeUniform13;
	float nodeUniform14;
	float nodeUniform15;
	float nodeUniform17;
	float nodeUniform18;
	float nodeUniform19;
	mat4 nodeUniform21;
	mat3 nodeUniform22;
};


// varyings
out vec3 nodeVarying0;
out vec2 nodeVarying1;
out float nodeVarying2;
out vec3 nodeVarying3;
out vec3 nodeVarying4;
out vec2 nodeVarying5;


// attributes
layout( location = 0 ) in vec3 position;
layout( location = 1 ) in vec2 uv;
layout( location = 2 ) in vec4 color;
layout( location = 3 ) in vec3 normal;


// vars
vec2 nodeVar6;
vec3 nodeVar13;
vec3 nodeVar17;
vec3 nodeVar25;
mat4 modelViewMatrix;
vec4 nodeVar35;
float nodeVar36;
vec4 nodeVar37;

// codes


void main() {

	// transforms


	// flow
	// code

	nodeVarying5 = vec2( 0.0, 1.0 );
	nodeVar6 = vec2( 0.0, 0.0 );
	nodeVarying1 = nodeVar6;
	nodeVar13 = vec3( 0.0, 0.0, 0.0 );
	nodeVarying3 = nodeVar13;
	nodeVar17 = vec3( 0.0, 0.0, 0.0 );
	nodeVarying4 = nodeVar17;
	nodeVar25 = vec3( 0.0, 0.0, 0.0 );
	nodeVarying0 = nodeVar25;
	nodeVarying2 = 1.0;
	modelViewMatrix = ( cameraViewMatrix * nodeUniform21 );
	nodeVar35 = ( modelViewMatrix * vec4( position, 1.0 ) );
	nodeVarying0 = vec3( 1.0, 1.0, 1.0 );
	nodeVarying1 = uv;

	if ( ( ( color.w < 1e30 ) && ( color.w > -1e30 ) ) ) {

		nodeVar36 = min( max( color.w, 0.0 ), 1.0 );

	} else {

		nodeVar36 = 1.0;

	}

	nodeVarying2 = nodeVar36;
	nodeVarying3 = nodeVar35.xyz;
	nodeVarying4 = ( cameraViewMatrix * vec4( ( nodeUniform22 * normal ), 0.0 ) ).xyz;
	nodeVar37 = ( cameraProjectionMatrix * nodeVar35 );
	nodeVarying5 = nodeVar37.zw;

	// result
	gl_Position = nodeVar37;

	gl_PointSize = 1.0;

}
