#version 300 es

// Three.js r185 - Node System


// extensions


// precision

precision highp float;
precision highp int;
precision highp sampler2D;
precision highp sampler3D;
precision highp samplerCube;
precision highp sampler2DArray;

precision highp usampler2D;
precision highp usampler3D;
precision highp usamplerCube;
precision highp usampler2DArray;

precision highp isampler2D;
precision highp isampler3D;
precision highp isamplerCube;
precision highp isampler2DArray;

precision highp sampler2DShadow;
precision highp sampler2DArrayShadow;
precision highp samplerCubeShadow;


// structs

layout( location = 0 ) out vec4 fragColor;



// uniforms

layout( std140 ) uniform object {
	vec2 nodeUniform0;
	float nodeUniform1;
	float nodeUniform2;
	float nodeUniform3;
	float nodeUniform5;
	mat3 nodeUniform7;
	uint nodeUniform8;
	mat3 nodeUniform9;
	uint nodeUniform10;
	mat3 nodeUniform11;
	uint nodeUniform12;
	mat3 nodeUniform13;
	uint nodeUniform14;
	mat4 nodeUniform16;
	float nodeUniform17;
	float nodeUniform18;
	vec2 nodeUniform19;
	float nodeUniform20;
	float nodeUniform21;
	float nodeUniform22;
	float nodeUniform23;
	float nodeUniform24;
	float nodeUniform25;
	float nodeUniform27;
	float nodeUniform30;
	float nodeUniform31;
	float nodeUniform32;
};

layout( std140 ) uniform render {
	float cameraNear;
	float cameraFar;
	mat4 cameraViewMatrix;
	mat4 cameraProjectionMatrix;
	mat4 cameraProjectionMatrixInverse;
};


// varyings
in float nodeVarying0;
in vec3 nodeVarying1;
in vec2 nodeVarying2;
in vec4 v_clipSpace;
in float nodeVarying4;
in vec2 nodeVarying5;


// vars
vec2 nodeVar0;
vec2 nodeVar2;
float nodeVar4;
float nodeVar5;
float gsplatPickMahalSq;
float gsplatPickIntensity;
float gsplatPickBrightness;
float nodeVar6;
vec4 nodeVar88;
vec3 positionView;
vec4 DiffuseColor;
vec4 Output;
vec4 nodeVar91;

// codes


void main() {

	// flow
	// code

	nodeVar0 = vec2( vec2( gl_FragCoord.xy.x, nodeUniform0.y - gl_FragCoord.xy.y ).x, ( nodeUniform0.y - vec2( gl_FragCoord.xy.x, nodeUniform0.y - gl_FragCoord.xy.y ).y ) );
	nodeVar2 = ( nodeVar0 - nodeVarying2 );
	nodeVar4 = ( nodeVar2.x * nodeVarying1.x );
	nodeVar5 = ( ( nodeVar2.y - ( nodeVarying1.y * nodeVar4 ) ) * nodeVarying1.z );
	gsplatPickMahalSq = 0.0;
	gsplatPickMahalSq = ( ( nodeVar4 * nodeVar4 ) + ( nodeVar5 * nodeVar5 ) );
	gsplatPickIntensity = 0.0;
	gsplatPickIntensity = ( ( nodeVarying0 * nodeUniform1 ) * max( ( exp( ( gsplatPickMahalSq * -0.5 ) ) - nodeUniform2 ), 0.0 ) );
	gsplatPickBrightness = 0.0;
	gsplatPickBrightness = clamp( gsplatPickIntensity, 0.0, 1.0 );

	if ( ( int( nodeUniform3 ) == 1 ) ) {

		nodeVar88 = ( cameraProjectionMatrixInverse * v_clipSpace );
		positionView = ( nodeVar88.xyz / vec3( nodeVar88.w ) );
		nodeVar6 = ( ( positionView.z + cameraNear ) / ( cameraNear - cameraFar ) );

	} else {

		nodeVar6 = ( 1.0 - gsplatPickBrightness );

	}

	gl_FragDepth = nodeVar6;

	if ( ( gsplatPickMahalSq > nodeUniform30 ) ) {

		discard;


	}


	if ( ( gsplatPickIntensity < 0.0001 ) ) {

		discard;


	}

	DiffuseColor = vec4( nodeVarying4, nodeVarying5.x, gsplatPickBrightness, nodeVarying5.y );
	DiffuseColor.w = ( DiffuseColor.w * nodeUniform32 );
	nodeVar91 = max( vec4( DiffuseColor.xyz, DiffuseColor.w ), 0.0 );
	Output = nodeVar91;

	// result
	fragColor = nodeVar91;

}
