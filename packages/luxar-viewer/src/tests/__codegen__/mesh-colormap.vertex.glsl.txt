#version 300 es

// Three.js r184 - Node System


// extensions


// precision

precision highp float;
precision highp int;
precision highp sampler2D;
precision highp sampler3D;
precision highp samplerCube;
precision highp sampler2DArray;

precision highp usampler2D;
precision highp usampler3D;
precision highp usamplerCube;
precision highp usampler2DArray;

precision highp isampler2D;
precision highp isampler3D;
precision highp isamplerCube;
precision highp isampler2DArray;

precision highp sampler2DShadow;
precision highp sampler2DArrayShadow;
precision highp samplerCubeShadow;


// uniforms

layout( std140 ) uniform render {
	mat4 cameraViewMatrix;
	mat4 cameraProjectionMatrix;
};

layout( std140 ) uniform object {
	float nodeUniform0;
	float nodeUniform1;
	float nodeUniform2;
	float nodeUniform3;
	float nodeUniform4;
	float nodeUniform5;
	float nodeUniform6;
	float nodeUniform7;
	float nodeUniform8;
	mat4 nodeUniform10;
	mat3 nodeUniform12;
	float nodeUniform13;
	float nodeUniform14;
	float nodeUniform15;
	uint nodeUniform16;
	mat3 nodeUniform17;
};
uniform sampler2D nodeUniform11;

// varyings
out vec3 nodeVarying0;
out float nodeVarying1;
out vec3 nodeVarying2;
out vec3 nodeVarying3;


// attributes
layout( location = 0 ) in vec3 position;
layout( location = 1 ) in float aScalar;
layout( location = 2 ) in vec4 color;
layout( location = 3 ) in vec3 normal;


// vars
vec3 nodeVar0;
vec3 nodeVar4;
vec3 nodeVar11;
mat4 modelViewMatrix;
vec4 nodeVar20;
vec2 nodeVar21;
vec2 nodeVar22;
bool nodeVar23;
vec2 nodeVar24;
vec4 nodeVar25;
float nodeVar26;
vec4 nodeVar27;

// codes


void main() {

	// transforms


	// flow
	// code

	nodeVar0 = vec3( 0.0, 0.0, 0.0 );
	nodeVarying2 = nodeVar0;
	nodeVar4 = vec3( 0.0, 0.0, 0.0 );
	nodeVarying3 = nodeVar4;
	nodeVar11 = vec3( 0.0, 0.0, 0.0 );
	nodeVarying0 = nodeVar11;
	nodeVarying1 = 1.0;
	modelViewMatrix = ( cameraViewMatrix * nodeUniform10 );
	nodeVar20 = ( modelViewMatrix * vec4( position, 1.0 ) );
	nodeVar21 = ( nodeUniform12 * vec3( vec2( pow( clamp( ( ( aScalar - nodeUniform13 ) * nodeUniform14 ), 0.0, 1.0 ), nodeUniform15 ), 0.5 ), 1.0 ) ).xy;
	nodeVar23 = bool( nodeUniform16 );

	if ( nodeVar23 ) {

		nodeVar24 = nodeVar21;
		nodeVar22 = vec2( nodeVar24.x, 1.0 - nodeVar24.y );

	} else {

		nodeVar22 = nodeVar21;

	}

	nodeVar25 = texture( nodeUniform11, nodeVar22 );
	nodeVarying0 = nodeVar25.xyz;

	if ( ( ( color.w < 1e30 ) && ( color.w > -1e30 ) ) ) {

		nodeVar26 = min( max( color.w, 0.0 ), 1.0 );

	} else {

		nodeVar26 = 1.0;

	}

	nodeVarying1 = nodeVar26;
	nodeVarying2 = nodeVar20.xyz;
	nodeVarying3 = ( cameraViewMatrix * vec4( ( nodeUniform17 * normal ), 0.0 ) ).xyz;
	nodeVar27 = ( cameraProjectionMatrix * nodeVar20 );

	// result
	gl_Position = nodeVar27;

	gl_PointSize = 1.0;

}
