#version 300 es

// Three.js r185 - Node System


// extensions


// precision

precision highp float;
precision highp int;
precision highp sampler2D;
precision highp sampler3D;
precision highp samplerCube;
precision highp sampler2DArray;

precision highp usampler2D;
precision highp usampler3D;
precision highp usamplerCube;
precision highp usampler2DArray;

precision highp isampler2D;
precision highp isampler3D;
precision highp isamplerCube;
precision highp isampler2DArray;

precision highp sampler2DShadow;
precision highp sampler2DArrayShadow;
precision highp samplerCubeShadow;


// structs

layout( location = 0 ) out vec4 fragColor;



// uniforms

layout( std140 ) uniform object {
	float nodeUniform0;
	float nodeUniform1;
	float nodeUniform2;
	float nodeUniform3;
	float nodeUniform4;
	float nodeUniform5;
	float nodeUniform6;
	float nodeUniform7;
	float nodeUniform8;
	float nodeUniform9;
	float nodeUniform10;
	mat4 nodeUniform12;
	mat3 nodeUniform14;
	float nodeUniform15;
	float nodeUniform16;
	float nodeUniform17;
	uint nodeUniform18;
	mat3 nodeUniform19;
};


// varyings
in vec3 nodeVarying0;
in float nodeVarying1;
in vec3 nodeVarying2;
in vec3 nodeVarying3;


// vars
vec4 DiffuseColor;
vec3 nodeVar1;
vec3 nodeVar2;
vec3 nodeVar3;
float nodeVar5;
float nodeVar6;
vec3 nodeVar7;
vec3 nodeVar8;
vec3 nodeVar9;
vec3 nodeVar10;
float nodeVar11;
vec3 nodeVar13;
vec3 nodeVar14;
float nodeVar15;
float nodeVar16;
float nodeVar17;
float nodeVar18;
float nodeVar19;
vec4 Output;
vec4 nodeVar20;

// codes


void main() {

	// flow
	// code

	nodeVar1 = normalize( cross( dFdx( nodeVarying2 ), dFdy( nodeVarying2 ) ) );

	if ( ( nodeVar1.z < 0.0 ) ) {

		nodeVar2 = ( - nodeVar1 );

	} else {

		nodeVar2 = nodeVar1;

	}

	nodeVar3 = nodeVar2;
	nodeVar5 = dot( nodeVarying3, nodeVarying3 );

	if ( gl_FrontFacing ) {

		nodeVar6 = 1.0;

	} else {

		nodeVar6 = -1.0;

	}

	nodeVar7 = ( ( nodeVarying3 * vec3( inversesqrt( max( nodeVar5, 1e-12 ) ) ) ) * vec3( nodeVar6 ) );

	if ( ( ( nodeVar5 >= 1e-12 ) && ( nodeVar5 < 1e30 ) ) ) {

		nodeVar8 = nodeVar7;

	} else {

		nodeVar8 = nodeVar3;

	}

	nodeVar9 = nodeVar8;
	nodeVar10 = normalize( vec3( -0.35, 0.55, 0.75 ) );
	nodeVar11 = mix( nodeUniform0, 1.0, pow( clamp( ( ( dot( nodeVar9, nodeVar10 ) * 0.5 ) + 0.5 ), 0.0, 1.0 ), nodeUniform1 ) );
	nodeVar13 = max( ( ( nodeVarying0 * vec3( nodeUniform2 ) ) + vec3( nodeUniform3 ) ), vec3( 0.0, 0.0, 0.0 ) );
	nodeVar14 = ( ( nodeVar13 * vec3( nodeVar11 ) ) + vec3( ( nodeUniform4 * pow( max( dot( nodeVar9, normalize( ( nodeVar10 + vec3( 0.0, 0.0, 1.0 ) ) ) ), 0.0 ), nodeUniform5 ) ) ) );
	nodeVar15 = ( nodeVarying1 * nodeUniform6 );

	if ( ( int( nodeUniform7 ) == 1 ) ) {

		nodeVar16 = 1.0;

	} else {


		if ( ( nodeVarying2.z >= 0.0 ) ) {

			nodeVar17 = 0.0;

		} else {

			nodeVar18 = max( nodeUniform8, 1e-20 );
			nodeVar17 = smoothstep( nodeVar18, ( nodeVar18 * 2.0 ), ( - nodeVarying2.z ) );

		}

		nodeVar16 = nodeVar17;

	}

	nodeVar19 = nodeVar16;

	if ( ( nodeVar19 < 0.01 ) ) {

		discard;


	}


	if ( ( nodeVar15 < nodeUniform9 ) ) {

		discard;


	}

	DiffuseColor = vec4( ( nodeVar14 * vec3( nodeVar19 ) ), 1.0 );
	DiffuseColor.w = ( DiffuseColor.w * nodeUniform10 );
	nodeVar20 = max( vec4( DiffuseColor.xyz, DiffuseColor.w ), 0.0 );
	Output = nodeVar20;

	// result
	fragColor = nodeVar20;

}
