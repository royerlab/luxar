{
  "note": "Scope is the default Playwright project. Imported parameter defaults and helper bodies are modelled, with helper traversal failing closed beyond ten nested calls and active recursion skipped. beforeAll/afterAll hooks remain out of scope. Deadlines are modelled as the largest single wait, not their sum.",
  "exceptions": []
}
