{
  "note": "Scope is the default Playwright project. Imported parameter defaults from ./helpers and ./helpers/* are modelled; deadlines inside helper bodies, local or imported, remain out of scope (see #2901). beforeAll/afterAll hooks are checked once each against their separate project timeout or a hook-local test.setTimeout/testInfo.setTimeout. Deadlines are modelled as the largest single wait, not their sum.",
  "exceptions": []
}
