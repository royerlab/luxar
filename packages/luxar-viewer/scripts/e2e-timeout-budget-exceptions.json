{
  "note": "Scope is the default Playwright project. Imported parameter defaults from ./helpers and ./helpers/* are modelled; deadlines inside helper bodies, local or imported, and beforeAll/afterAll hooks remain out of scope (see #2901). Deadlines are modelled as the largest single wait, not their sum.",
  "exceptions": []
}
